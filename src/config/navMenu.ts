import type { NavMenu } from "@/types";

export const navMenu: NavMenu = {
  links: [
    {
      title: 'FrontEnd',
      href: '/frontend'
    },
    {
      title: 'Backend',
      href: '/backend'
    }
  ]
}